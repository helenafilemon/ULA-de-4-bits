module bin5_para_bcd (
    input  wire [4:0] bin_in,
    output reg  [3:0] bcd_dezenas,
    output reg  [3:0] bcd_unidades
);

    
    always @(*) begin
        bcd_dezenas = bin_in / 10;
        bcd_unidades = bin_in % 10;
    end

endmodule
